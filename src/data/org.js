/**
 * Canonical organisation naming. Import these rather than retyping the name,
 * so the official form stays consistent everywhere it appears.
 *
 * `name` is the official name and should be used wherever the club is formally
 * identified. `shortName` is the brand mark used in the nav, footer lockup, and
 * casual references.
 */

export const org = {
  name: "Computational Sustainability Club at Cornell",
  shortName: "Sustainabytes",
  university: "Cornell University",
  founded: 2024,
};
